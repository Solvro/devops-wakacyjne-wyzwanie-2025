image:
  repository: nginx
  tag: "latest"

replicaCount: 3

service:
  type: ClusterIP
  port: 80

config:
  appMode: "production"
  logLevel: "info"

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 50